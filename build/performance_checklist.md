# 性能检查清单（Performance Checklist）

> 项目：xing's blog · 更新：2026-07-28
> 图例：❌ 待优化 · ⚠️ 部分达成 · ✅ 已达成

## 1. 资源体积

- [x] ✅ 图片压缩（2026-07-28 P1）：9 张每日一图 JPG 4.2MB → WebP(q80, ≤1920px, 去EXIF) 1.3MB；友邻头像 316K → 85K
- [ ] ⚠️ 图片尺寸：已限最大边 1920px；响应式 `srcset` 多尺寸未做（低优先级）
- [ ] ⚠️ 图片懒加载：友邻头像已加 `loading="lazy"`，每日一图未加
- [x] ✅ 重复资源：`src/image/` 下重复副本已删除（2026-07-28）；`public/` 仍有一份 832K 无引用壁纸，待确认后删除
- [x] ✅ 图片命名：统一为语义化 `daily-NN.webp`（2026-07-28）

## 2. CSS

- [x] ✅ 重复定义已清除（2026-07-28 P1）：删除 288 行完全重复区块，`.daily-view-init` 选择器 8处→3处，CSS 1266→954 行
- [ ] ⚠️ 遗留：`.sidebar` 新旧两版本块（~147/~312 行）属性覆盖纠缠，需人工合并+视觉回归（P1-5b）
- [ ] ⚠️ 单一全局 CSS（1266 行）全页面加载；静态博客体量下可接受，后续可按需拆分
- [x] ✅ 动画使用 `transform`/`opacity`（GPU 友好）
- [x] ✅ 使用 CSS 变量，便于主题维护

## 3. JavaScript

- [x] ✅ 无重型前端框架，仅少量内联原生 JS（IIFE 隔离作用域）
- [x] ✅ 客户端 `innerHTML.replace` hack 已删除（2026-07-28）：Astro 7 Satteri 处理器内置 GFM，构建期原生渲染 `<del>`
- [ ] ❌ 文章覆层 fetch 整页 HTML 再 DOMParser 解析；可考虑只渲染正文片段（低优先级）
- [x] ✅ 滚动监听使用 `{ passive: true }`

## 4. 页面与加载

- [x] ✅ 卡片链接已改真实 href + 事件委托渐进增强（2026-07-28 P1）：可爬取、无 JS 可跳转、支持中键新标签
- [ ] ❌ 无资源预加载：首屏关键图（每日一图/背景）未 `preload`
- [x] ✅ 纯静态 SSG，无运行时服务端开销
- [x] ✅ favicon 提供 svg + ico 双格式

## 5. 缓存与部署（部署侧确认）

- [ ] ❌ 静态资源长缓存策略（`dist/_astro/` 带 hash 文件可 `immutable` 缓存 1 年）
- [ ] ❌ 未确认是否启用 gzip/brotli 压缩（取决于托管平台）
- [ ] ❌ 未配置 CDN

## 6. 度量基准（优化前记录）

| 指标 | 当前值 | 目标 |
|------|--------|------|
| `src/image/` 总体积 | 1.3 MB（原 5.0 MB，P1 已达成） | < 1.5 MB |
| 单张最大图 | 360 KB（原 900 KB） | < 300 KB |
| 全局 CSS 行数 | 954（原 1266；sidebar 合并后预计 ~800） | ~800 |
| Lighthouse Performance | 未测 | ≥ 95 |
| `dist/` 产物体积 | 2.5 MB（原 5.2 MB） | < 2 MB（剩 public/ 832K 壁纸待确认） |

## 每次优化后必做

1. `npm run build` 确认构建通过
2. 对比 `dist/` 体积：`du -sh dist/`
3. 更新本清单勾选状态与度量表
4. 在 `build/changelog.md` 追加记录
